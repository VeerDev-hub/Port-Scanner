services:
  web:
    image: nginx:alpine
    ports:
      - "18080:80"

  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_USER: postgres
      POSTGRES_DB: testdb
    ports:
      - "15432:5432"
